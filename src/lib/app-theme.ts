/** Keep in sync with `--background` in `src/app/globals.css`. */
export const APP_BACKGROUND_COLOR = "#f4f7f6";

/** Keep in sync with `.dark --background` in `src/app/globals.css`. */
export const APP_DARK_BACKGROUND_COLOR = "#0b1419";
